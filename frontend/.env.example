# Frontend environment example for HireHive

# Firebase Configuration is loaded directly from firebaseConfig.js
# No environment variables needed for Firebase in this setup

# Backend GraphQL API endpoint
REACT_APP_GRAPHQL_URL=http://localhost:4000/graphql

# Socket.IO endpoint for real-time notifications
REACT_APP_SOCKET_URL=http://localhost:4000
